build:
 icec-auth:  # cambiar por su namespace de okteto
   image: okteto.dev/icec-auth:latest  # cambiar por su namespace de okteto

deploy:
 - okteto build # build the image with the latest code
 - kubectl apply -f manifests # create all the resources in kubernetes
dev:
 icec-auth:
   command: bash
   sync:
   - .:/api-auth   # cambiar por su proyecto