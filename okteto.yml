build:
 icecapi-yoangelcruz:  # cambiar por su namespace de okteto
   image: okteto.dev/icec-yoangelcruz:latest  # cambiar por su namespace de okteto

deploy:
 - okteto build # build the image with the latest code
 - kubectl apply -f manifests # create all the resources in kubernetes
dev:
 icecapi-yoangelcruz:
   command: bash
   sync:
   - .:/api-auth   # cambiar por su proyecto